# Extra Drivers

This directory is for optional, non-SST39 chip descriptors.

Policy:
- Do not block SST39 core milestones with extra driver work.
- Each extra descriptor must validate against `drivers/schema/chip-driver.schema.json`.
- Each extra descriptor must declare capability coverage and known limitations.
