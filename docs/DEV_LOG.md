# DEV Log

This file records implemented steps, experiments, investigations, cleanups, and other execution history that should not live in the master plan.

## Usage
- Add short chronological entries.
- Log what was done.
- Keep durable planning knowledge out of this file; move that to `docs/MASTERPLAN.md`.

## Entries
- 2026-03-15: Historical implementation and release-setup steps existed previously inside `MASTERPLAN.md`; they were intentionally moved out so the master plan can stay focused on durable planning truth.
- 2026-03-15: Rebuilt firmware and reflashed BluePill using PlatformIO HID uploader (`upload_protocol = hid`). Upload completed successfully; the serial node remained `/dev/ttyACM1` in this run, but the setup is treated as dynamic (`/dev/ttyACM*` can change between runs).
- 2026-03-15: Added and executed ROM-less serial protocol smoke test (`FlashBangFirmware/scripts/protocol_smoke.py`) on `/dev/ttyACM1`. Results: parser and framing paths respond as expected (`?`, `READ`, malformed parse, range check), but write/erase/status operations unexpectedly returned `OK` without a connected ROM, indicating false-positive completion detection in no-chip conditions.
- 2026-03-15: Implemented no-chip guard in command execution path (`FlashBangFirmware/src/command_executor.cpp`) by requiring a supported SST39 ID (`0xBF/0xB5..0xB7`) before `PROGRAM_BYTE`, `WRITE_STATUS`, `SECTOR_ERASE`, and `CHIP_ERASE`. Reflashed BluePill and re-ran `protocol_smoke.py`; all guarded commands now return `ERR|E_HW|chip not detected` in ROM-less setup.
- 2026-03-15: Flashed firmware to real target setup (ROM physically connected) via HID on `/dev/ttyACM1`; upload succeeded. Post-flash protocol checks (`ID`, `READ`) returned `mf=0x0,dev=0x0,name=unknown,size=0` and all-zero data at address `0x00000`, while guarded mutating commands correctly remained blocked with `ERR|E_HW|chip not detected`.
- 2026-03-15: Milestone reached: dumped first `4096` bytes from attached ROM to binary file `captures/rom_first_4096.bin` using `FlashBangFirmware/scripts/dump_rom_range.py`. File size verified as `4096` bytes; initial sample bytes read back as all `0x00`.
- 2026-03-15: Re-test after reseating ROM (possible prior mis-insertion): read-only diagnostics now report stable `mf=0xFF, dev=0xFF` (still unknown), single-byte probes all `0xFF`, and fresh dump `captures/rom_first_4096_retry.bin` is all `0xFF`. This indicates bus/read path state changed from previous all-`0x00` run but still no valid SST39 ID.
- 2026-03-15: With Winbond `W29EE011-15` inserted for ID-only testing, read-only diagnostics produced unstable non-SST39 values (`0x41`, `0x7B`, `0x7F`) and non-repeatable probe bytes across repeated reads. No stable JEDEC ID observed, indicating remaining signal/wiring/timing issues rather than a valid recognized chip response.
- 2026-03-15: Updated ID logic to enforce strict vendor-specific command sequences: SST entry `AA/55/90`, Winbond entry `AA/55/80/AA/55/60`, and explicit exit `AA/55/F0` (instead of single `F0` write). Reflashed and retested with Winbond; IDs remain unstable and mirrored (`mf == dev`), so protocol-order issue is addressed but hardware signaling still unstable.
- 2026-03-15: Refactored detection architecture: moved multi-chip probe logic into dedicated module (`FlashBangFirmware/src/chip_probe.cpp`), reverted `FlashBangFirmware/src/sst39_ops.cpp` to SST-only operations, and changed `ID` response to include selected `driver=`. Added descriptor `drivers/extras/winbond-w29ee011.yaml` (`0xDAC1`) to keep chip metadata in YAML registry.
- 2026-03-15: Added continuous data-pin monitor test mode in firmware with explicit start/stop commands (`DATA_BUS_MONITOR_START`, `DATA_BUS_MONITOR_STOP`). During monitor mode, firmware emits `STATUS|DATA_BUS|SAMPLE|0|<binary D7..D0>` frames and remains stoppable via serial command; validated live after flash.
- 2026-03-15: Enabled internal STM32 pulldowns on data bus input mode (`PB8..PB15` via `INPUT_PULLDOWN`) to reduce floating reads. Build/flash succeeded and diagnostics changed from mostly `0xFF` noise to dominant `0x00/0x7E` patterns, but ID detection remains unstable (`mf==dev`, no known SST39/Winbond match), indicating unresolved hardware/timing issues.
- 2026-03-15: Hardened parser for serial terminal interoperability: command lines are now trimmed and uppercased before parsing (CRLF/leading-trailing whitespace/case tolerant). Verified with `"  data_bus_monitor_start\r\n"` and `"DATA_BUS_MONITOR_STOP\r\n"` after flash.
- 2026-03-15: Fixed data-bus configuration path to guarantee `INPUT_PULLDOWN` is applied at least once even when initial mode is already input. After flash, diagnostics became fully stable but stuck at constant `0x7E` across all probe addresses/reads (ID `0x7E/0x7E`), indicating deterministic external drive/stuck wiring rather than random floating.
- 2026-03-15: Added early SWJ/peripheral pin release in firmware setup (`AFIO` + `SWJ_DISABLE`, disable SPI1/I2C1/USART1/USART2 clocks) and tested revised read/write bus cycle ordering. Result on current setup remained stable invalid reads (constant `0xFF`, ID `0xFF/0xFF`) after flash.
- 2026-03-15: Tuned read sequencing (set address before OE low) and increased ID settle delay (`WAIT_ID_MODE_US=10`). Result still `0xFF`-stuck for ID and read probes, indicating remaining mismatch versus known-good legacy firmware behavior.
- 2026-03-15: Added monitor-address command `SET_A#####` (`00000..7FFFF`) for targeted address selection during `DATA_BUS_MONITOR_START`. Firmware now stores the selected 19-bit address, applies it before each monitor sample, and includes the address in status payloads as `STATUS|DATA_BUS|SAMPLE|0|A=<addr>,D=<bits>`.
- 2026-03-15: Hardened address-bus initialization to avoid accidental float states: replaced range-based pin loops with explicit pin lists, added `initAddressBusPins()`, and enforced address pins as outputs before each `busSetAddress()` write. Post-flash `ADDR_BUS_TEST|A0_7` changed from all-high float-like failures to structured mismatch (`mismatches=128`, first mismatch `expected=0x10 observed=0x20`), indicating active driving with likely bit-line mapping/wiring error rather than full bus float.
- 2026-03-15: After harness correction and re-inserting SST39, reflashed firmware and re-ran protocol smoke. ID detection now succeeds as `mf=0xBF, dev=0xB7, name=SST39SF040, size=524288, driver=sst39-core`, confirming functional SST39 probe path with current bus initialization changes.
- 2026-03-15: Restored explicit `HELLO` command handling in firmware parser/executor (responds with `HELLO|<fw>|0.1|<caps>`), and hardened host GUI behavior so chip info is only shown after a successful real `ID` parse (`mf/dev` recognized). GUI now clears stale mock chip data on serial connect before live identification.
- 2026-03-15: Hardened `FlashBangFirmware/scripts/protocol_smoke.py` to be read-only by default; destructive commands (`PROGRAM_BYTE`, erase operations) now run only with explicit `--destructive` flag to prevent accidental ROM data loss during routine checks.
- 2026-03-29: Added GUI architecture and developer guide documentation in `docs/Gui.md` (overview, detailed module/file responsibility map, and modification workflows with examples for operations, color logic, protocol integration, and tab/layout changes).
- 2026-03-29: Extended `docs/Gui.md` with a dedicated layout deep dive including ASCII sketches (overall window stack, Hex Workspace grid composition, and operation data flow), layout hotspots in `gui.rs`, and concrete change recipes for geometry and panel extension work.
- 2026-03-29: Corrected GUI layout implementation to target `P / (R-G-B) / (Y-C) / S`: moved status into top info row, kept transfer column centered between Inspector and Workbench with 50/50 side split, added adaptive transfer width under narrow windows, and tightened hex-cell spacing/sizing so both Inspector and Workbench panels stay visible on screen.
- 2026-03-30: Restructured repository layout to root-level app and firmware folders (`FlashBangStudio/`, `FlashBangFirmware/`) and removed the redundant `host/` layer. Updated affected CI/workflow/docs paths, corrected `FlashBangStudio/build.rs` repo-root resolution (`..`), fixed GUI asset include paths after the move, and renamed the chip snapshot UI area from Monitor/ChipView to Inspector while keeping protocol command names with `MONITOR` unchanged for compatibility.
- 2026-03-30: Closed remaining post-restructure release issue: fixed `.github/workflows/release.yml` firmware build step to use `working-directory: FlashBangFirmware` (previously stale `firmware` path), and aligned `docs/RELEASE_WORKFLOW.md` local validation command accordingly.
- 2026-03-30: Simplified GUI as requested: removed ChipInfo/Diff tabs and Diff export buttons from top bar, rendered only Hex Workspace in main area, removed section titles above R/G/B/Y/C groups, enforced icon operation buttons as exact `120x40` image buttons, and moved compact chip identification details into the top status/info line.
- 2026-03-30: Refined Hex Workspace geometry to match target panel model: compact transfer column (`~150px` including padding for 120x40 buttons), strict equal width for Inspector/Workbench columns, and per-column vertical stacking so `chipactions` width equals Inspector width and `diskactions` width equals Workbench width.
- 2026-04-03: Removed family-specific chip identification from firmware ID path. `probeChipInfo()` now only executes loaded ID sequences and returns raw `mf/dev`; hardcoded SST39 model mapping and `hasSupportedSst39()` gating were removed.
- 2026-04-03: Neutralized operation naming in active firmware code path (`driverProgramByte` / `driverSectorErase` / `driverChipErase`), renamed operation module files to `driver_ops.*`, and verified build success.
- 2026-04-03: Added CLI helper `FlashBangFirmware/scripts/driver_id_probe.py` to upload driver parameters/sequences from YAML and then query `ID`; validated Winbond probe response as `mf=0xda,dev=0xc1,name=unknown,size=0,driver=unknown`.
- 2026-04-03: Started FlashBangStudio implementation for protocol `0.4` compatibility and host-side chip resolution. Updated Studio parsing paths to accept `mf/dev` (with legacy alias support), enforced HELLO protocol-version check for `0.4`, aligned mock responses to `HELLO|...|0.4|driver-upload` and `OK|ID|mf=...,dev=...`, and integrated a first YAML driver catalog lookup (`drivers/chips/*.yaml`) for ID->model resolution in host code.
- 2026-04-03: Continued FlashBangStudio GUI workflow and UX integration: serial monitor switched to selectable/cuttable text area, active sector marker color changed to dark red, connect workflow changed to `HELLO -> driver upload -> ID` with explicit driver selection/refresh in GUI, and read/fetch path hardened with host-side bounds checks to prevent out-of-range `READ` requests.
- 2026-04-03: Fixed a concrete `READ` range mismatch scenario in FlashBangStudio where host-resolved chip size (from ID/catalog) could diverge from firmware `CHIP_SIZE` after wrong driver upload, causing firmware `ERR|E_RANGE|read range out of bounds` on full-image fetch. Added uploaded-driver tracking and strict mismatch blocking (`uploaded driver` must match `detected driver`) before fetch/read operations.
- 2026-04-03: Fixed SST39 family upload sizing bug in FlashBangStudio: pre-ID upload had used the first model size from YAML (`0x20000` / 128 KiB), which caused `READ|...|524288` to fail with firmware `E_RANGE` on SST39SF040. Upload plan now sets `CHIP_SIZE` to the largest model size before ID, and GUI sends an explicit post-ID `PARAMETER|CHIP_SIZE|<exact detected size>` update.
- 2026-04-03: Consolidated durable findings from serial parser/paste/burst investigations into `docs/MASTERPLAN.md` for future GUI/protocol work. Added explicit planning guidance to keep at least two chip descriptors for GUI tests and to treat manual terminal paste as non-authoritative compared to scripted serial checks.
- 2026-04-03: Captured burst-mode conclusion in master planning: under load, TX response lines may arrive truncated/fragmented even when RX command ingestion remains stable; GUI/host handling must remain resilient to incomplete lines.
- 2026-04-04: Implemented phased async action migration in `FlashBangStudio/src/gui.rs`: introduced dispatcher/worker channel scaffolding, event-drain integration in GUI update loop, and worker-thread execution slices for `QueryFirmware`, `UploadDriverAndId`, `FetchImage/FetchRange/FetchSector`, and `FlashImage/FlashRange/FlashSector`.
- 2026-04-04: Added worker-side serial helpers for command/OK handling and verify readback, plus patch-based result application on GUI side (data/known/received/logs). Flash completion now applies auto-fetch patches or marks affected ranges stale when auto-fetch is disabled.
- 2026-04-04: Removed obsolete synchronous flash/readback helpers after migration (`read_single_byte`, `read_single_byte_stable`, `flash_range_from_work`), then validated with `cargo check` and `cargo test --bin flashbang-studio -q` (15 tests passed).
- 2026-04-05: Migrated erase operations to worker-thread execution in `FlashBangStudio/src/gui.rs` (`EraseImage`, `EraseSector`): added serial worker request/event flow, worker-side erase helpers with optional auto-fetch patch return, and GUI-thread reconciliation identical to fetch/flash.
- 2026-04-05: Removed now-obsolete synchronous erase methods and simplified deferred-action execution return path (`ActionExecutionState` cleanup). Validation: `cargo check` clean and `cargo test --bin flashbang-studio -q` with 15/15 passing.
- 2026-04-05: Added compact sync status badges in Hex Workspace for fully synced states (`IMAGE GEFLASHT`, `SEKTOR GEFLASHT`). Badge criteria require known/fresh Inspector bytes plus byte-identical content; image badge additionally requires exact chip-size alignment between Inspector and Workbench.
- 2026-04-05: Flash button gating tightened via `flash_disable_reason`: flash operations are now disabled when the selected image/range/sector is already fully known and identical (`Bereich bereits identisch (alles geflasht)`), avoiding redundant programming runs.
- 2026-04-05: Reworked sync badges in Hex Workspace to permanent compact status chips `IMAGE` and `SECTOR` (never hidden). Badge color now always reflects aggregate diff state (`GRUEN`, `ORANGE`, `ROT`, `GRAU`) and hover tooltip explains detailed state/counts and invalid-geometry reasons.
- 2026-04-05: Top-bar cleanup: removed `Desktop GUI Preview` text, switched visible version to Cargo package version (`CARGO_PKG_VERSION`) for automatic correctness, removed Input-Mode text display, and replaced `Refresh Ports`/`Refresh Driver` text buttons with compact reload-symbol buttons (`⟳`) directly next to the corresponding dropdowns (with tooltips).
- 2026-04-05: Split host actions previously conflated in `Firmware abfragen` / `Upload Driver + ID`: new separate operations are `ID` (only ID read + apply), `Upload Driver` (only YAML parameters/sequences upload), and `Driver Abfragen` (only `INSPECT`). `HELLO` remains separate and no longer auto-triggers driver upload.
- 2026-04-05: Refined workflow semantics as requested: removed manual `HELLO` button from top bar; `Connect` now owns the sequence (`open -> HELLO -> ID-check via selected driver ID-sequences -> Upload Driver -> size/init checks -> optional Auto-Fetch`). `ID` remains a fast side-effect-free ID check (no auto-fetch, no implicit full driver upload).
- 2026-04-05: Removed obsolete synchronous serial helper methods in Studio GUI after worker migration (`serial_send_and_read_lines`, `send_expect_ok`, `dump_range_to_ro`) and kept logic on worker-handle helpers only; validated with `cargo check` and `cargo test --bin flashbang-studio -q` (15/15).
- 2026-04-05: Updated `docs/Gui.md` with a complete current button/function inventory (one-line usage situations) and corrected outdated protocol/UI references to the current worker-based command split.
- 2026-04-05: Removed redundant GUI save-format dropdowns (Image/Sector) from Hex Workspace lower panel because format selection is already handled in the Save dialog flow.
- 2026-04-05: Centralized Studio expected protocol version in `FlashBangStudio/src/version.rs` and reused it from GUI HELLO validation and `session.rs` handshake checks, removing duplicated hardcoded protocol literals.
- 2026-04-05: Updated About dialog fields to display stable component information (`Studio` package version, expected protocol version, build metadata/tag/git), reducing stale/mismatched version display risk.
- 2026-04-05: Added master-plan governance rule that any Firmware/Protocol/Studio version bump must be tagged on the same commit using `F0.0.0-P0.0.0-S0.0.0`.
- 2026-04-05: Refined Studio build metadata automation in `FlashBangStudio/build.rs`: About fields are now generated from automated sources with clear semantics (`Local version`, `Based on release` from last tag on HEAD first-parent line, `Most recent commit`, `dirty`, `Build date` as `yyyymmdd-hhmm`, `Build number`, `Minimum protocol version`).
- 2026-04-05: Added Studio semver guard in build script: if local `CARGO_PKG_VERSION` and `Based on release` are both valid semver triplets, local must be greater than release baseline, otherwise build fails.
- 2026-04-05: Updated About labels in GUI to avoid ambiguity between local development version and release baseline; unknown Git/tag/date values now fall back to `unknown`.
- 2026-04-05: Clarified and locked versioning policy: firmware version may grow independently of protocol version for bugfix/maintenance releases; protocol version is changed only for host/device contract milestones. Protocol patch level is expected to be frequently `0` but is not a hard rule.
- 2026-04-05: Coordinated release bump prepared to `F0.5.0-P0.5.0-S0.5.0`: updated `FlashBangFirmware/VERSION`, shared protocol version source `protocol/VERSION`, Studio package version in `FlashBangStudio/Cargo.toml`, and protocol spec headline in `protocol/flashbang-link-protocol.md`; validated with `cargo check` and `pio run`.